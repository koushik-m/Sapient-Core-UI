// (function(str){
//     return str.slice(1,-1);
// })("koushik"); //any string can be passed in place of koushik

function editstring(str) {
    return str.slice(1,-1);
}
//call the editstring with a string parameter.